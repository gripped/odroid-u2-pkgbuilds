# Maintainer: Georges Discry <georges at discry dot be>

pkgname=mali-odroidu2
pkgver=r1p1.r3p2.01rel3
pkgrel=1
pkgdesc="mali EGL libs and xorg video driver"
arch=('armv7h')
url=""
license=('MIT')
depends=('')
makedepends=( 'X-ABI-VIDEODRV_VERSION=14')
conflicts=('xorg-server<1.14.0' 'X-ABI-VIDEODRV_VERSION<14' 'X-ABI-VIDEODRV_VERSION>=15')
options=('!libtool')
groups=('xorg-drivers' 'xorg')
provides=('libump' 'mali-odroidu2')
source=('maliodroid.tar.xz')
md5sums=('SKIP')






package() {
  cd "${srcdir}"/
  cp -av etc usr ${pkgdir}
}
